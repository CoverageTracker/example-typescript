{
  "name": "example-typescript",
  "version": "1.0.0",
  "private": true,
  "description": "Reference invoicing/cart library for Coverage Tracker's JS/TS example (LCOV via vitest).",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "coverage": "vitest run --coverage --coverage.reporter=lcov",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.4",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
